# The TaskMate Project

Комплексная система управления задачами, включающая веб-интерфейс для управления процессами.

## 📂 Структура репозитория

```text
.
├─ docker-compose.yml                       # Общие настройки сервисов
├─ docker-compose.override.yml              # Локальные правки (порты, pgadmin)
├─ docker-compose.prod.yml                  # Прод-параметры (SSL, Nginx)
├─ nginx/
│   ├─ nginx.local.conf                     # Nginx для локальной разработки
│   └─ nginx.prod.conf                      # Nginx для продакшна (SSL)
├─ TaskMateClient/                        # Frontend (React 19, TypeScript 5.9, Vite 7)
├─ TaskMateServer/                         # Backend API (Laravel 12, PHP 8.4, FrankenPHP)
└─ ...
```

---

## 🚀 Развертывание на локальной машине (Full Guide)

Следуйте этой инструкции **пошагово**, чтобы развернуть проект с нуля.

### 0. Предварительные требования

- **OS**: Linux (Ubuntu Recommended) / macOS / Windows (WSL2)
- **Docker Engine** (не используйте `snap` версию в Ubuntu!)
- **Git**

### 1. Клонирование репозитория

Проект содержит подмодули, поэтому клонируйте с флагом `--recursive`:

```bash
git clone --recursive https://github.com/xierongchuan/TaskMate.git .
# Если уже склонировали без submodule:
# git submodule update --init --recursive
```

### 2. Настройка окружения (.env)

Скопируйте пример конфига в рабочий файл:

```bash
cp .env.example .env
```

**Обязательно** откройте файл `.env` и настройте следующие параметры для локального запуска:

```dotenv
# App URLs (важно для Docker сети)
LARAVEL_APP_URL=http://localhost:8007
VITE_API_BASE_URL=http://localhost:8007/api/v1

# Database Credentials
DB_USERNAME=postgres
DB_PASSWORD=secret
```

### 3. Запуск контейнеров

Запустите сборку и старт контейнеров:

```bash
docker compose up -d --build
```

> ⏳ *Первый запуск может занять время (сборка PHP и JS образов).*

### 4. Инициализация Backend

После того как контейнеры запустились (проверьте через `docker compose ps`), выполните настройку бэкенда.

**А) Установка зависимостей (Composer):**

Если папка `vendor` отсутствует (код монтируется, но зависимости в .gitignore), установите их внутри контейнера:

```bash
docker compose exec backend_api composer install
```

**Б) Миграции и Сидинг (Тестовые данные):**

Накатываем структуру БД и заполняем её демо-пользователями:

```bash
docker compose exec backend_api php artisan migrate --force
docker compose exec backend_api php artisan db:seed-demo
```

**В) Линк хранилища:**

Чтобы картинки и файлы были доступны из веба:

```bash
docker compose exec backend_api php artisan storage:link
```

### 5. Готово! 🏁

Проект доступен по адресам:

- **Frontend**: [http://localhost:8099](http://localhost:8099)
- **Backend API**: [http://localhost:8007](http://localhost:8007)

**Данные для входа (из Demo Data):**

- **Admin**: `admin` / `password`
- **Manager**: `manager1` / `password`
- **Employees**: `emp1_1` / `password` (и другие)

---

## 🛠 Технологический стек

### Frontend

- **Framework**: React 19
- **Build Tool**: Vite 7
- **Language**: TypeScript 5.9
- **Styling**: Tailwind CSS 3.4
- **State**: Zustand
- **Query**: TanStack Query v5 (с оптимизацией UX через `placeholderData`)

### Backend API

- **Framework**: Laravel 12
- **Language**: PHP 8.4
- **Application Server**: FrankenPHP v1 (Caddy-based)
- **Testing**: Pest PHP 3.8 (193+ тестов, 50%+ покрытие)
- **Database**: PostgreSQL 18
- **Cache**: Valkey (Redis-compatible)
- **Storage**: Local (S3-ready)

> **FrankenPHP** — современный сервер приложений для PHP, построенный на Caddy. Обеспечивает лучшую производительность по сравнению с PHP-FPM, встроенную поддержку HTTP/2/HTTP/3, автоматический HTTPS и сжатие (zstd/gzip).

### Архитектура Backend

Проект использует многослойную архитектуру:

- **Controllers** — тонкие контроллеры с PHPDoc, делегируют логику сервисам
- **Form Requests** — валидация входных данных (`StoreTaskRequest`, `UpdateTaskRequest`, `StoreUserRequest`, `UpdateUserRequest`)
- **Services** — бизнес-логика:
  - `TaskService` — создание, обновление задач, проверка дубликатов
  - `TaskFilterService` — фильтрация задач по параметрам
  - `TaskProofService` — загрузка и валидация доказательств выполнения
  - `DashboardService` — оптимизированные агрегатные запросы
  - `ShiftService` — управление сменами
  - `SettingsService` — системные настройки
- **API Resources** — форматирование ответов API
- **Custom Exceptions** — `DuplicateTaskException`, `AccessDeniedException`
- **SoftDeletes** — мягкое удаление на User, AutoDealership, Task

### Типы задач и верификация

Система поддерживает три типа задач:

1. **notification** — уведомление без требования ответа
2. **completion** — требует отметки о выполнении
3. **completion_with_proof** — требует загрузки доказательств (до 5 файлов, до 200 МБ)

**Workflow верификации:**

- Сотрудник отмечает задачу выполненной → `pending_review`
- Менеджер проверяет → `verified` / `rejected`
- Приватное хранилище с подписанными URL (60 мин)
- Трёхуровневая безопасность: URL подпись + Bearer токен + проверка доступа

---

## ⏰ Очереди и Планировщик (Scheduler)

Для корректной работы генераторов задач и фоновых процессов (уведомления, отчеты) в проекте используется сложная система очередей.

### 1. Планировщик (Scheduler)

Работает в отдельном контейнере `backend_scheduler` и запускает команды по расписанию:

- **Генерация задач (`ProcessTaskGeneratorsJob`)**: Каждые 5 минут. Создает экземпляры задач из активных генераторов.
- **Повторяющиеся задачи (`ProcessRecurringTasksJob`)**: Ежечасно.
- **Уведомления и проверки**: Настроены в `routes/console.php`.

Механизм запуска: Скрипт `sync-scheduler.sh` обеспечивает запуск `php artisan schedule:run` ровно в `XX:XX:00` каждую минуту для точности.

### 2. Очереди (Queues)

Обработчик очередей `laravel-worker` (Supervisor) настроен на обработку двух каналов:

1. `default` — основные задачи (генерация задач, системные джобы).
2. `notifications` — отправка уведомлений (приоритетная очередь).

Конфигурация Supervisor: `TaskMateServer/supervisor.conf`.

---

## ✨ Unified UI & Принципы разработки

Проект активно переходит на систему **Unified UI**, обеспечивающую единый визуальный стиль и поддержку **Dark Mode**.

### Основные компоненты (библиотека `components/ui`)

- `PageContainer` — обертка страниц с отступами и центрированием.
- `Card` — универсальные контейнеры для контента.
- `Button` / `IconButton` — кнопки с поддержкой состояний загрузки и иконок.
- `Input` / `Select` / `Checkbox` / `Textarea` — стандартизированные поля ввода.
- `Badge` — статусы и метки (унифицированные цвета: success, danger, gray и др.).
- `Pagination` — компонент пагинации (интегрирован в Tasks и Users).
- `ConfirmDialog` — замена стандартных `window.confirm`.
- `Toast` — неблокирующие уведомления.
- `Skeleton` / `EmptyState` / `ErrorState` — управление состояний загрузки и ошибок.
- `FilterPanel` — унифицированная панель фильтрации.

### Страницы, мигрированные на Unified UI

- ✅ **Задачи** (`TasksPage`)
- ✅ **Архив задач** (`ArchivedTasksPage`)
- ✅ **Генераторы задач** (`TaskGeneratorsPage`)
- ✅ **Автосалоны** (`DealershipsPage`)
- ✅ **Настройки уведомлений** (`NotificationSettingsPage`)
- ✅ **Пользователи** (`UsersPage` — добавлена пагинация)
- ✅ **Настройки** (`SettingsPage` — частичная миграция и фиксы UX)

---

## ⚠️ Правила разработки (Internal)

### Backend

1. **Тесты**: При любых изменениях **ОБЯЗАТЕЛЬНО** запускать тесты:

   ```bash
   docker compose exec backend_api php artisan test
   ```

2. **Актуальность тестов**: При изменении логики проверять, что существующие тесты отражают новое поведение.
3. **Документация**: После успешного внедрения изменений обновлять `README.md`.

### Frontend & API

1. **Совместимость**: При изменении Backend проверять совместимость с Frontend и документацией API.
2. **API-контракт**: При изменении Frontend сверяться с документацией API и проверять корректность запросов.
3. **UX**: Использовать `placeholderData: (prev) => prev` в TanStack Query, чтобы избежать мерцания интерфейса.

---

## 📂 Troubleshooting (Частые проблемы)

### Permission denied (Docker Build/Runtime)

Если возникает ошибка `permission denied` при работе с `storage/framework/testing` или другими директориями, это связано с конфликтом прав между вашим пользователем на хосте и пользователем `www-data` внутри контейнера.

**Решение (рекомендуемое):**

```bash
# 1. Установить права на весь проект (код защищен)
chmod -R 755 /home/temur/Projects/TaskMate/TaskMateServer

# 2. Разрешить запись в директории storage и bootstrap/cache
chmod -R 775 /home/temur/Projects/TaskMate/TaskMateServer/storage
chmod -R 775 /home/temur/Projects/TaskMate/TaskMateServer/bootstrap/cache

# 3. Установить права на файлы внутри этих директорий
find /home/temur/Projects/TaskMate/TaskMateServer/storage -type f -exec chmod 664 {} \;
find /home/temur/Projects/TaskMate/TaskMateServer/bootstrap/cache -type f -exec chmod 664 {} \;

# 4. КРИТИЧНО: Добавить вашего пользователя в группу www-data
sudo usermod -a -G www-data $USER

# 5. Применить изменения группы (или перелогиниться)
newgrp www-data
```

**Объяснение прав:**

- `755` (rwxr-xr-x) — для кода: владелец может читать/писать/выполнять, остальные только читать
- `775` (rwxrwxr-x) — для storage/cache: владелец и группа могут писать (контейнер сможет создавать файлы)
- `664` (rw-rw-r--) — для файлов: владелец и группа могут читать/писать

Это безопасное решение, которое:

- ✅ Позволяет контейнерам (www-data) создавать и изменять файлы
- ✅ Позволяет вашему пользователю редактировать код
- ✅ Защищает код от случайного изменения (нет 777)
- ✅ Не дает избыточных прав "всем пользователям"

### Database connection

Убедитесь, что в `.env` указан `DB_HOST=postgres`, а не `localhost`.

---

## Лицензия

License: Proprietary License
Copyright: © 2023-2026 [https://github.com/xierongchuan](https://github.com/xierongchuan). All rights reserved.
