# The TaskMate Project

Комплексная система управления задачами, включающая Telegram-бота для сотрудников и веб-интерфейс для управления процессами.

## 📂 Структура репозитория

```text
.
├─ docker-compose.yml                       # Общие настройки сервисов
├─ docker-compose.override.yml              # Локальные правки (порты, pgadmin)
├─ docker-compose.prod.yml                  # Прод-параметры (SSL, Nginx)
├─ nginx/
│   ├─ nginx.local.conf                     # Nginx для локальной разработки
│   └─ nginx.prod.conf                      # Nginx для продакшна (SSL)
├─ TaskMateFrontend/                        # Frontend (React 19, TypeScript 5.9, Vite 7)
├─ TaskMateTelegramBot/                     # Backend & Bot (Laravel 12, PHP 8.4)
├─ TaskMateAPI/                             # Коллекция API (Bruno)
└─ ...
```

---

## 🚀 Развертывание на локальной машине (Full Guide)

Следуйте этой инструкции **пошагово**, чтобы развернуть проект с нуля.

### 0. Предварительные требования

- **OS**: Linux (Ubuntu Recommended) / macOS / Windows (WSL2)
- **Docker Engine** (не используйте `snap` версию в Ubuntu!)
- **Git**

### 1. Клонирование репозитория

Проект содержит подмодули, поэтому клонируйте с флагом `--recursive`:

```bash
git clone --recursive https://github.com/xierongchuan/TaskMate.git .
# Если уже склонировали без submodule:
# git submodule update --init --recursive
```

### 2. Настройка окружения (.env)

Скопируйте пример конфига в рабочий файл:

```bash
cp .env.example .env
```

**Обязательно** откройте файл `.env` и настройте следующие параметры для локального запуска:

```dotenv
# App URLs (важно для Docker сети)
LARAVEL_APP_URL=http://localhost:8007
TASK_MATE_TELEGRAM_BOT_APP_URL=http://localhost:8007
VANILLA_FLOW_TELEGRAM_BOT_APP_URL=http://localhost:8007
VITE_API_BASE_URL=http://localhost:8007/api/v1

# Database Credentials
DB_USERNAME=postgres
DB_PASSWORD=secret

# Security Keys (можно сгенерировать любые случайные строки или base64)
# Пример генерации: openssl rand -base64 32
TASK_MATE_TELEGRAM_BOT_APP_KEY=base64:YOUR_GENERATED_KEY_HERE

# Telegram Bot (для локальных тестов можно использовать заглушку)
TASK_MATE_TELEGRAM_BOT_TOKEN="123456:FAKE-TOKEN-FOR-LOCAL-DEV"
```

### 3. Запуск контейнеров

Запустите сборку и старт контейнеров:

```bash
docker compose up -d --build
```

> ⏳ *Первый запуск может занять время (сборка PHP и JS образов).*

### 4. Инициализация Backend

После того как контейнеры запустились (проверьте через `docker compose ps`), выполните настройку бэкенда.

**А) Установка зависимостей (Composer):**

Если папка `vendor` отсутствует (код монтируется, но зависимости в .gitignore), установите их внутри контейнера:

```bash
docker compose exec src_telegram_bot_api composer install
```

**Б) Миграции и Сидинг (Тестовые данные):**

Накатываем структуру БД и заполняем её демо-пользователями:

```bash
docker compose exec src_telegram_bot_api php artisan migrate --force
docker compose exec src_telegram_bot_api php artisan db:seed
```

**В) Линк хранилища:**

Чтобы картинки и файлы были доступны из веба:

```bash
docker compose exec src_telegram_bot_api php artisan storage:link
```

### 5. Готово! 🏁

Проект доступен по адресам:

- **Frontend**: [http://localhost:8099](http://localhost:8099)
- **Backend API**: [http://localhost:8007](http://localhost:8007)

**Данные для входа (из Demo Data):**

- **Admin**: `admin` / `password`
- **Manager**: `manager1` / `password`
- **Employees**: `emp1_1` / `password` (и другие)

---

## 🛠 Технологический стек

### Frontend

- **Framework**: React 19
- **Build Tool**: Vite 7
- **Language**: TypeScript 5.9
- **Styling**: Tailwind CSS 3.4
- **State**: Zustand
- **Query**: TanStack Query v5 (с оптимизацией UX через `placeholderData`)

### Backend & Bot

- **Framework**: Laravel 12
- **Language**: PHP 8.4
- **Testing**: Pest PHP 3.8
- **Bot SDK**: Nutgram 1.5
- **Database**: PostgreSQL 18
- **Cache**: Valkey (Redis-compatible)

---

## ✨ Unified UI & Принципы разработки

Проект активно переходит на систему **Unified UI**, обеспечивающую единый визуальный стиль и поддержку **Dark Mode**.

### Основные компоненты (библиотека `components/ui`)

- `PageContainer` — обертка страниц с отступами и центрированием.
- `Card` — универсальные контейнеры для контента.
- `Button` / `IconButton` — кнопки с поддержкой состояний загрузки и иконок.
- `Input` / `Select` / `Checkbox` / `Textarea` — стандартизированные поля ввода.
- `Badge` — статусы и метки (унифицированные цвета: success, danger, gray и др.).
- `Pagination` — компонент пагинации (интегрирован в Tasks и Users).
- `ConfirmDialog` — замена стандартных `window.confirm`.
- `Toast` — неблокирующие уведомления.
- `Skeleton` / `EmptyState` / `ErrorState` — управление состояний загрузки и ошибок.
- `FilterPanel` — унифицированная панель фильтрации.

### Страницы, мигрированные на Unified UI

- ✅ **Задачи** (`TasksPage`)
- ✅ **Архив задач** (`ArchivedTasksPage`)
- ✅ **Генераторы задач** (`TaskGeneratorsPage`)
- ✅ **Автосалоны** (`DealershipsPage`)
- ✅ **Настройки уведомлений** (`NotificationSettingsPage`)
- ✅ **Пользователи** (`UsersPage` — добавлена пагинация)
- ✅ **Настройки** (`SettingsPage` — частичная миграция и фиксы UX)

---

## 🛠 API & Документация (Bruno)

В папке `TaskMateAPI` находится коллекция для **Bruno** (Open Source клиент для API).

1. Установите [Bruno](https://www.usebruno.com/).
2. Откройте папку `TaskMateAPI` как новую коллекцию.
3. Используйте предустановленные запросы для Auth, Tasks, Users и др.

---

## ⚠️ Правила разработки (Internal)

1. **Backend**: При любых изменениях **ОБЯЗАТЕЛЬНО** запускать тесты:

   ```bash
   docker compose exec src_telegram_bot_api php artisan test
   ```

2. **Frontend & Backend**: Всегда проверять совместимость с API (Swagger/Bruno).
3. **UX**: При сохранении данных использовать `placeholderData` в TanStack Query, чтобы избежать мерцания интерфейса (метод `(prev) => prev`).
4. **README**: После каждого успешного внедрения функционала обновлять `README.md`.

---

## 📂 Troubleshooting (Частые проблемы)

- **Permission denied (Docker Build)**: Если при сборке возникает ошибка доступа к `storage/framework/testing`, выполните на хосте:

  ```bash
  sudo chown -R $USER:$USER TaskMateTelegramBot/storage
  ```

- **Storage permissions (Runtime)**: Если бот не может писать логи:

  ```bash
  docker compose exec src_telegram_bot_api chown -R www-data:www-data storage bootstrap/cache
  ```

- **Database connection**: Убедитесь, что в `.env` указан `DB_HOST=postgres`, а не `localhost`.

---

## Лицензия

License: Proprietary License  
Copyright: © 2023-2026 [谢榕川](https://github.com/xierongchuan) All rights reserved.
